import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        fontSize: 32,
        color: '#4681A3',
        fontWeight: 'bold',
    }
});

export default styles;
