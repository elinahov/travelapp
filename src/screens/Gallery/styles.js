import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    image: {
        width: '100%',
        height: 400,
        borderRadius: 20,
        marginTop: 24,
    },
    backContainer: {
        position: 'absolute',
        margin: 32,
    },
    backIcon: {
        width: 40,
        height: 40,
    }
});

export default styles;
